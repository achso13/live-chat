import React from "react";
import ReactDOM from "react-dom/client";
import "../css/app.css";
import "./bootstrap";

ReactDOM.createRoot(document.getElementById("app")).render(
    <React.StrictMode>
        <h1>Hi, this is ReactJS with Laravel</h1>
    </React.StrictMode>
);
